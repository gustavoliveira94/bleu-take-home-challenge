{
  "name": "@bleu-builders/tech-challenge-contracts",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "anvil": "anvil",
    "deploy:local": "forge script script/Deploy.s.sol:Deploy --rpc-url $RPC_URL --private-key $PRIVATE_KEY --broadcast",
    "deploy:testnet": "forge script script/Deploy.s.sol:Deploy --rpc-url $RPC_URL --private-key $PRIVATE_KEY --broadcast --verify",
    "clean": "forge clean"
  }
}